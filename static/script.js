console.log("Traffic Signal Dashboard Loaded");

// Auto-refresh every 30 seconds
setTimeout(() => {
    window.location.reload();   // tells the browser to refresh the current page.
}, 30000); //30000 is the delay in milliseconds (30,000 ms = 30 seconds).
